<p align="center">
  <img src="./fav/android-chrome-192x192.png" width="88" height="88" alt="Task Tracer 图标">
</p>

<h1 align="center">Task Tracer 任务跟踪器</h1>

<p align="center"><strong>一个单文件、本地优先、可安装离线使用的个人任务、计划与提醒应用。</strong></p>

<p align="center">
  <a href="https://todo.muquew.com/" rel="noopener noreferrer">在线体验</a> · <a href="./README.md">English README</a>
</p>

<p align="center">
  <a href="https://todo.muquew.com/" rel="noopener noreferrer"><img alt="在线体验" src="https://img.shields.io/badge/Live-Demo-2563eb?style=flat-square"></a>
  <img alt="单文件" src="https://img.shields.io/badge/App-Single%20HTML-0f766e?style=flat-square">
  <img alt="PWA" src="https://img.shields.io/badge/PWA-Installable%20%7C%20Offline-059669?style=flat-square">
  <img alt="多语言" src="https://img.shields.io/badge/i18n-ZH%20%7C%20EN-7c3aed?style=flat-square">
  <img alt="无障碍" src="https://img.shields.io/badge/A11y-Keyboard%20%7C%20Screen%20Reader-d97706?style=flat-square">
  <img alt="许可" src="https://img.shields.io/badge/License-Personal%20Use-475569?style=flat-square">
</p>

Task Tracer 是一个面向个人使用的任务工作台。它可以作为可安装的网页应用运行，任务数据保存在当前浏览器中，适合日常任务捕获、项目跟进、重复事项、本地提醒、任务归档和个人效率回顾。

它适合学习计划、个人事务、独立项目、例行复盘、截止日期工作，以及任何更重视本地掌控、清晰状态和数据可导出的任务管理场景。

## 可以做什么

| 需求 | Task Tracer 支持 |
| --- | --- |
| 快速记录任务 | 完整任务表单、快速添加解析、截止日期/时间、无日期任务、子任务、项目和标签。 |
| 管理不断增长的列表 | 项目分组、多标签标记、限定筛选、智能搜索、保存智能视图、归档历史和手动排序。 |
| 按时间规划 | 列表、日历、时间线、实时倒计时、状态片段、进度条和跨时区稳定日期。 |
| 执行今天的任务 | 将任务加入今日计划，进入专注执行模式，也可以不改变任务本身地移出今日计划。 |
| 批量处理 | 选择当前可见任务，并批量完成、归档、删除或加入今日计划。 |
| 处理例行事项 | 每天、每周、指定星期、每月、每月最后一天和自定义周期重复。 |
| 获得提醒 | 浏览器提醒、提前提醒、重复提醒、稍后提醒、错过提醒提示和后台提醒限制说明。 |
| 回顾完成情况 | 完成率、进行中任务逾期率、归档数量、今日完成数、连续完成天数和近期趋势。 |
| 保护数据 | 撤销最近任务操作、导出/备份、导入预览、合并/替换冲突处理、备份健康状态和存储受限时的紧急备份。 |
| 舒服地使用 | 响应式布局、明暗主题、减少动态效果、键盘导航、读屏标签和中英文界面。 |

## 日常工作流

1. 使用完整弹窗添加任务，或用快速添加输入一条轻量任务。
2. 对常用搜索和筛选组合保存智能视图，形成稳定入口。
3. 在列表视图中完成、编辑、稍后提醒、归档、删除、处理子任务、批量操作和手动排序。
4. 把当前要做的任务加入今日计划，需要执行时使用顶部今日计划按钮进入专注模式。
5. 当日期排布比列表顺序更重要时，切换到日历视图或时间线视图。
6. 在统计视图中查看当前负载、完成表现、归档历史和近期状态。
7. 在更换浏览器、清理站点数据或测试导入文件前，先使用导出/备份下载 JSON 文件。

## 快速添加

快速添加可以把轻量文本解析成结构化任务字段。

```text
明天 20:00 复习英语 #学习 /个人
```

这会创建一个名为“复习英语”的任务，截止到明天 20:00，标签为“学习”，项目为“个人”。

命令面板使用 `Ctrl/Cmd + P` 打开，可以新建任务、聚焦快速添加、切换视图、打开保存的智能视图、进入今日计划、进入批量操作、撤销上一步、导出/备份、搜索和跳转项目。

## 项目、标签与搜索

项目是主要分组。每个任务属于一个项目，例如 `工作`、`个人` 或某个具体计划。项目选择器可以收窄任务空间，全部项目列表会按项目保持分组。

标签是灵活的辅助标记。一个任务可以有多个标签，例如 `学习`、`设计`、`复盘`。标签适合连接不同项目中的同类任务。

搜索会匹配任务名称、任务描述、项目名称、标签和子任务文本。限定搜索可以和普通关键词一起使用：

| 输入 | 匹配内容 |
| --- | --- |
| `project:工作` | 项目名包含“工作”的任务。 |
| `tag:学习` 或 `#学习` | 拥有对应标签的任务。 |
| `status:active` | 进行中的未完成任务。 |
| `status:completed` | 已完成任务。 |
| `status:archived` | 已归档任务。 |
| `status:overdue` | 已过期且未完成的任务。 |
| `status:no-deadline` | 无截止日期任务。 |
| `status:repeat` | 重复任务。 |
| `due:today` | 今天截止的任务。 |
| `due:tomorrow` | 明天截止的任务。 |
| `due:week` | 未来七天内截止的任务。 |
| `due:2025-05-20` | 指定本地日期截止的任务。 |
| `due:no-deadline` | 无截止日期任务。 |
| `project:工作 tag:报告` | 同时满足两个限定条件的任务。 |

如果项目名或标签名包含空格，可以搜索其中有区分度的一部分。

保存智能视图会记录当前搜索、项目、状态筛选、排序方式和视图。它适合固定工作范围，例如 `project:工作 status:overdue`、`tag:学习 due:week`，或某个项目的日历视图。

## 今日计划、批量操作与撤销

今日计划是轻量执行层，顶部靶心按钮可以直接打开。把任务加入今日计划不会改变项目、标签、截止日期、归档状态或重复规则，只是把它标记为今天要处理的任务。

批量操作模式会在当前可见列表中显示选择框。选中的任务可以一起加入今日计划、标记完成、归档或删除。

最近的任务写入操作可以从通知条、应用菜单，或在焦点不在输入框内时使用 `Ctrl/Cmd + Z` 撤销。撤销会恢复操作前的任务快照。

## 视图

| 视图 | 适合做什么 |
| --- | --- |
| 列表 | 日常执行任务，包含完整操作、子任务、状态片段、提醒、归档控制和手动排序。 |
| 日历 | 按真实月历查看日期安排，支持上一月/下一月、上一年/下一年、回到今天、日期详情和无日期分组。 |
| 时间线 | 按日期顺序浏览即将到来或历史阶段的任务。 |
| 统计 | 查看完成率、进行中任务逾期率、归档数量、今日完成数、当前连续完成天数和近期趋势。 |

## 重复任务与提醒

重复任务支持每天、每周、指定星期、每月、每月最后一天和自定义天数周期。重复任务可以暂停，也可以跳过本次，而不需要删除重复规则。

当浏览器支持通知且用户授予权限时，Task Tracer 可以在应用打开期间检查提醒、按设定间隔重复提醒、将提醒稍后处理，并在应用重新运行时提示错过的提醒。

浏览器提醒不是系统级闹钟服务。提醒是否准时投递会受到浏览器策略、操作系统行为、电池设置、标签页生命周期，以及应用是否处于打开或被唤醒状态影响。

## 导入与导出/备份

| 动作 | 用途 |
| --- | --- |
| 导出/备份 | 下载带导出 ID、完整性校验、版本说明的完整任务 JSON，并更新最近备份时间。 |
| 导入 | 写入前先预览 JSON 文件，包含恢复检查清单、导入差异、任务数量、重复 ID、同名重复项、当前数据影响和同名冲突处理选项。 |

恢复检查清单会识别 Task Tracer 备份元数据、模板兼容状态、完整性校验状态、任务内容、重复 ID、本地同名项和替换当前数据的影响。如果替换导入会覆盖现有任务，Task Tracer 会先自动下载导入前快照，并等待第二次确认。完整性校验失败时，替换导入也需要显式风险确认；合并模式仍可用于更稳妥地恢复数据。

备份健康状态会显示在应用菜单中，用来提示今天已备份、最近已备份，或建议重新备份。

## 安装、离线与更新

Task Tracer 是 PWA，也就是浏览器支持时可以像应用一样安装和离线打开的网页应用。应用菜单中提供安装入口。如果当前浏览器没有自动安装弹窗，同一个入口会显示桌面 Chrome/Edge、Android Chrome 和 iOS Safari 的手动安装方式。

应用外壳在缓存后可以离线加载。当有新的离线版本准备好时，Task Tracer 会显示更新提示，并提供刷新更新操作。

## 数据与隐私

任务数据保存在当前浏览器的 IndexedDB 中。Task Tracer 不需要账号，默认不会把任务内容上传到服务器。

如果浏览器阻止本地存储，Task Tracer 会进入存储不可用模式，而不是假装更改已经保存。页面仍保留内存任务快照时，请先下载紧急备份，再刷新或关闭页面。调整浏览器隐私设置后，可以使用“重试”重新检测本地存储。

更换浏览器、清理站点数据、重新安装应用或迁移设备前，请先使用“导出/备份”下载 JSON 文件。

## 截图

| 任务列表 | 添加任务 |
| --- | --- |
| <img src="./screenshots/task-list.png" alt="Task Tracer 任务列表界面"> | <img src="./screenshots/add-task.png" alt="Task Tracer 添加任务弹窗"> |

## 运行方式

直接访问在线地址：

```text
https://todo.muquew.com/
```

或本地运行：

```bash
git clone https://github.com/muquew/Task-Tracer.git
cd Task-Tracer
python3 -m http.server 8080
```

然后打开：

```text
http://127.0.0.1:8080/
```

## 许可

Task Tracer 采用个人非商业使用许可。个人任务管理、学习、研究和评估可以使用；商业使用、付费分发或集成到商业服务需要获得 `muquew` 的事先书面授权。

完整条款见 [LICENSE](./LICENSE)。
